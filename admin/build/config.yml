version: '3'

info:
  companyName: "yonlycode"
  productName: "tyyy-admin"
  productIdentifier: "com.yonlycode.tyyy-admin"
  description: "yo-port admin CMS"
  copyright: "© 2026, yonlycode"
  comments: "Desktop & mobile admin CMS for yo-port"
  version: "0.1.0"

dev_mode:
  root_path: .
  log_level: warn
  debounce: 1000
  ignore:
    dir:
      - .git
      - node_modules
      - frontend
      - bin
    file:
      - .DS_Store
      - .gitignore
      - .gitkeep
      - "*_test.go"
    watched_extension:
      - "*.go"
      - "*.js"
      - "*.ts"
    git_ignore: true
  executes:
    - cmd: wails3 build DEV=true
      type: blocking
    - cmd: wails3 task common:dev:frontend
      type: background
    - cmd: wails3 task run
      type: primary
